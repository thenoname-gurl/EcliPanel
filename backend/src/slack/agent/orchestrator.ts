// Slack bot agent removed — see PR #
export async function runAgent() { return { status: "removed", reply: "Slack bot has been removed." }; }
export async function continueAgent() { return { status: "removed", reply: "Slack bot has been removed." }; }
export function clearPendingContinuation() {}
